- hosts: localhost
  connection: local
  gather_facts: false
  vars:
    # .. note::
    #    The followings cause
    #    ansible.errors.AnsibleParserError: Invalid variable name ...
    # foo-: 1
    # 0_bar_1: aaa
    # a b c: abc
    # フー_バー_バズ: foo
    フー_バー_バズ:
      - 1
      - 2
  tasks:
    - name: Try ping
      ping:
